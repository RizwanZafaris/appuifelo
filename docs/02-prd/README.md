# Stage 3 — PRD

> 🟡 **Pending Review Gate 2 approval.** Will be filled at Stage 3.

Target file: `prd-onboarding.md` with the 8-phase journey:

1. **Identity** — welcome, signup-method, OTP
2. **Context** — region (IP-detected, overridable), name
3. **Permissions** — SMS / notifications / location / contacts (each skippable)
4. **Financial profile** — earning type, accounts, investments
5. **Intent** — budget, 2 goals (hard limit, redesigned card-grid UI)
6. **Remittance** — corridor (skipped if single-region domestic)
7. **Personalization** — loading screen with user-specific status messages
8. **Dashboard handoff** — contract definition (dashboard impl is out of scope)

Plus global rules: state persistence, back navigation, conditional progress
bar, validation patterns, out-of-scope.
